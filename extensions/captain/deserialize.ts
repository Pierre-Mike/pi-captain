// Re-export from core — kept for backwards compatibility with existing importers.
// New code should import directly from "./core/deserialize.js".
export { deserializeRunnable } from "./core/deserialize.js";
